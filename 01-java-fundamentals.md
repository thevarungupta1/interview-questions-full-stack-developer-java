
---

##  **Java Core & Fundamentals**

###  **Basic Concepts**

1. What is the difference between JDK, JRE, and JVM?
2. What are the main features of Java?
3. Why is Java platform-independent?
4. Explain Just-In-Time (JIT) compiler.
5. What is bytecode in Java?
6. What is the role of the `main()` method?
7. What are identifiers, literals, and keywords in Java?
8. Difference between `==` and `.equals()` in Java?
9. What is the difference between `final`, `finally`, and `finalize()`?
10. Explain wrapper classes in Java.

---

###  **Data Types & Variables**

11. What are primitive and non-primitive data types in Java?
12. What is type casting? Explain implicit and explicit casting.
13. What is autoboxing and unboxing?
14. What is a static variable and when to use it?
15. What happens if you don’t initialize a local variable?

---

###  **Control Flow & Operators**

16. Difference between `break` and `continue` statements.
17. Can we use `switch` with strings in Java?
18. How does the `for-each` loop work internally?
19. What are short-circuit operators (`&&` and `||`)?
20. What is the difference between `==`, `equals()`, and `compareTo()`?

---

###  **Arrays & Strings**

21. How are arrays stored in memory?
22. Difference between `String`, `StringBuilder`, and `StringBuffer`.
23. Why are Strings immutable in Java?
24. How to compare two strings ignoring case?
25. How does the `intern()` method work?

---

##  **OOPs (Object-Oriented Programming) in Java**

###  **Core Concepts**

26. What are the four main OOP principles?
27. Explain the concept of **encapsulation** with an example.
28. What is **inheritance**? What are its types in Java?
29. Explain **polymorphism** — compile-time vs runtime.
30. What is **abstraction**? How is it achieved in Java?

---

###  **Classes, Objects & Constructors**

31. What is the difference between a class and an object?
32. Explain constructor overloading with an example.
33. Can a constructor be private? If yes, where is it used?
34. What is a static block? When is it executed?
35. What happens if you define a return type in a constructor?

---

###  **Inheritance & Interfaces**

36. Can Java support multiple inheritance?
37. Difference between abstract class and interface.
38. Can interfaces have default and static methods?
39. How do you achieve multiple inheritance using interfaces?
40. Can an interface extend another interface?

---

###  **Polymorphism & Overriding**

41. What is method overloading vs overriding?
42. Can you override a static method?
43. What is dynamic method dispatch?
44. How does the JVM decide which overridden method to call?
45. What is covariant return type?

---

###  **Encapsulation & Access Modifiers**

46. What are access modifiers in Java?
47. Difference between `private`, `protected`, and `default` access.
48. What is the purpose of getters and setters?
49. Can a class be both abstract and final?
50. How can you achieve data hiding in Java?

---

##  **Advanced Core Concepts**

###  **Memory & Exception Handling**

51. How does Java manage memory (Heap vs Stack)?
52. What is garbage collection? Can you force it?
53. Difference between `throw` and `throws`.
54. What is the use of `try-with-resources`?
55. What happens if an exception is thrown in the `finally` block?

---

###  **Static & Inner Classes**

56. Difference between static and non-static inner classes.
57. What are anonymous inner classes?
58. Can you define a static method in an interface?
59. What are nested classes used for?
60. Can an interface extend a class?

---

###  **Miscellaneous**

61. Explain the difference between `this` and `super` keyword.
62. Can we have multiple public classes in a Java file?
63. Difference between shallow copy and deep copy.
64. Explain the object lifecycle in Java.
65. How does the `instanceof` operator work?

---

##  **Scenario-Based Questions**

66. If you mark a class as `final`, can it be extended?
67. If a parent class has a private method, can it be overridden?
68. What happens when you call `System.exit(0)` in a `try` block?
69. Can you create an object without using the `new` keyword?
70. Why is the main method static in Java?

---

##  **Advanced / Tricky Questions**

71. Can an abstract class have a constructor?
72. Why can’t we instantiate an interface?
73. Can we overload the `main()` method?
74. What is the difference between `this()` and `super()`?
75. What happens if we remove `public` from `main()`?
76. Can we declare an interface method as `protected`?
77. What are marker interfaces (like Serializable)?
78. Can an abstract class implement an interface without defining all methods?
79. What is the difference between composition and inheritance?
80. Explain immutability — how can we make a class immutable?

---
